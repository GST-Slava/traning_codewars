function oddCount(n) {
    return Math.floor(n / 2);
}//solutions codewars

//other solutions

const oddCount = n => Math.floor(n / 2);//1

function oddCount(n) {
    /*
var returnArray = [];
for(var i=1;i<n;i=i+2) {
returnArray.push(i);
}
return returnArray.length;
    */
    return parseInt(n / 2);
}